machine:
    node:
        version: '6.9.5'
notify:
    webhooks:
        - url: http://bot.otiswhite.space/hooks
deployment:
  production:
    branch: master
    commands:
      - firebase deploy --token "$FIREBASE_TOKEN" --non-interactive
